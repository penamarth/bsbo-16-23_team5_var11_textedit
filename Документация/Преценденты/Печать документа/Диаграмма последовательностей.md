```
sequenceDiagram
    actor User
    participant Editor
    participant Document
    participant PrintSettings
    participant PrintManager
    participant PrintPreview
    participant Printer

    User->>Editor: newDocument(name, path)
    Editor->>Document: create Document(name, path)
    Document-->>Editor: document

    User->>Editor: setPrintSettings(printer, copies, range, duplex, orientation)
    Editor->>PrintSettings: create PrintSettings(...)
    PrintSettings-->>Editor: settings

    User->>Editor: showPreview(document, settings)
    Editor->>PrintManager: showPreview(document, settings)
    PrintManager->>PrintPreview: generatePreview(document, settings)
    PrintPreview->>Document: getText()
    Document-->>PrintPreview: text
    PrintPreview-->>PrintManager: preview image
    PrintManager-->>Editor: preview image
    Editor-->>User: отобразить предпросмотр

    User->>Editor: printDocument(document, settings)
    Editor->>PrintManager: print(document, settings)
    PrintManager->>Printer: send_to_print(document, settings)
    Printer-->>PrintManager: printed
    PrintManager-->>Editor: подтверждение
    Editor-->>User: документ напечатан
```
